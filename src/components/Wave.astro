---
/* src/components/Wave.astro — Yukina와 동일한 구조/동작 */
---
<div class="waves" id="header-waves" aria-hidden="true">
  <svg
    class="waves"
    xmlns="http://www.w3.org/2000/svg"
    xmlns:xlink="http://www.w3.org/1999/xlink"
    viewBox="0 24 150 28"
    preserveAspectRatio="none"
    shape-rendering="auto"
  >
    <defs>
      <path
        id="gentle-wave"
        d="M-160 44c30 0 58-18 88-18s 58 18 88 18 58-18 88-18 58 18 88 18 v44h-352z">
      </path>
    </defs>
    <g class="parallax">
      <use xlink:href="#gentle-wave" x="48" y="0" class="opacity-25"  style="animation-delay:-2s; animation-duration:7s;"></use>
      <use xlink:href="#gentle-wave" x="48" y="3" class="opacity-50"  style="animation-delay:-3s; animation-duration:10s;"></use>
      <use xlink:href="#gentle-wave" x="48" y="5" class="opacity-75"  style="animation-delay:-4s; animation-duration:13s;"></use>
      <use xlink:href="#gentle-wave" x="48" y="7"                   style="animation-delay:-5s; animation-duration:20s;"></use>
    </g>
  </svg>
</div>

<style>
/* Yukina와 같은 레이아웃/모션 */
#header-waves {
  position:absolute; left:0; right:0; bottom:-1px;
  width:100%;
  height:10vh; max-height:9.375rem; min-height:3.125rem;
  pointer-events:none;
}
@media (min-width:768px){ #header-waves { height:15vh } }

#header-waves > .waves { width:100%; height:100%; display:block }

/* ▶ 배경색과 완전히 동일하도록 fill을 --page-bg로 고정
   (Fuwari가 html에 bg-[var(--page-bg)]를 쓰므로 다크모드도 자동 연동) */
.parallax use{
  fill: var(--page-bg);
  animation: wave 25s cubic-bezier(0.5,0.5,0.45,0.5) infinite;
  will-change: transform;
}
@keyframes wave{
  0%   { transform: translate3d(-90px,0,0) }
  100% { transform: translate3d( 85px,0,0) }
}

@media (prefers-reduced-motion: reduce){
  .parallax use { animation:none }
}
</style>
